function chartRow(n,v,total){return `<div class="chartRow"><div class="rowTop"><span>${n}</span><strong>${ils(v)} · ${pct(v,total)}%</strong></div><div class="chartBar"><i style="width:${pct(v,total)}%"></i></div></div>`}function deskHeader(title,sub){return `<div class="deskHeader"><div><h1>${title}</h1><p>${sub}</p></div><div class="deskActions"><button data-reset>איפוס עסקאות</button><button class="primary" data-action="add">＋ הוצאה חדשה</button></div></div>`}
function desktopView(){const gd=groupData(),tx=txs(),al=alerts(),f=flexTotals();return `<div class="desktopShell"><aside class="side"><div class="brand">KamaOut</div><div class="tag">Family finance cockpit</div><div class="sideNav"><button data-dtab="overview" class="${desktopTab==='overview'?'active':''}">סקירה חודשית</button><button data-dtab="budget" class="${desktopTab==='budget'?'active':''}">ניהול תקציב</button><button data-dtab="transactions" class="${desktopTab==='transactions'?'active':''}">כל ההוצאות</button><button data-dtab="settings" class="${desktopTab==='settings'?'active':''}">הגדרות ומקורות</button></div><div class="sideFoot">${state.household.name}<br>${monthLabel()}</div></aside><main class="deskMain"><section class="desktopPage ${desktopTab==='overview'?'active':''}">${deskHeader('סקירה חודשית','תמונת מצב מלאה של הבית, על מסך אחד')}<div class="kpiGrid"><div class="kpi"><span>הכנסות מתוכננות</span><strong>${ils(incomeBudget())}</strong><small>4 מקורות הכנסה</small></div><div class="kpi"><span>תקציב ריאלי</span><strong>${ils(expenseBudget())}</strong><small>${ils(hiddenBudget())} רזרבות וחבויות</small></div><div class="kpi"><span>הוצאות בפועל</span><strong>${ils(actualTotal())}</strong><small>${pct(actualTotal(),expenseBudget())}% מהתקציב</small></div><div class="kpi"><span>יתרה צפויה</span><strong>${ils(predictedBalance())}</strong><small>לאחר כל הרזרבות</small></div></div><div class="deskGrid"><div class="panel"><div class="liveHead"><h3>תקציב לפי נושא</h3><span class="liveDot"><i></i>חודש נוכחי</span></div><table class="budgetTable"><thead><tr><th>נושא</th><th>תקציב</th><th>בפועל</th><th>ניצול</th><th>מצב</th></tr></thead><tbody>${Object.entries(gd).map(([n,g])=>`<tr><td><strong>${n}</strong></td><td>${ils(g.budget)}</td><td>${ils(g.actual)}</td><td><div class="tinyProgress"><i style="width:${Math.min(100,pct(g.actual,g.budget))}%"></i></div></td><td>${g.actual>g.budget?'<span class="badge red">חריגה</span>':pct(g.actual,g.budget)>=80?'<span class="badge amber">מתקרב</span>':'<span class="badge ok">תקין</span>'}</td></tr>`).join('')}</tbody></table></div><div class="panel"><h3>חריגות והתראות</h3>${al.length?al.map(alertHtml).join(''):`<div class="emptyDesk">אין חריגות כרגע</div>`}<h3 style="margin-top:24px">גמישות התקציב</h3>${chartRow('קשיח',f.fixed,expenseBudget())}${chartRow('בינוני',f.medium,expenseBudget())}${chartRow('גמיש',f.high,expenseBudget())}</div></div><div class="panel" style="margin-top:16px"><div class="liveHead"><h3>הוצאות אחרונות</h3><span class="liveDot"><i></i>אותו מידע מכל המכשירים אחרי חיבור הענן</span></div>${tx.length?`<table class="budgetTable"><thead><tr><th>תאריך</th><th>עסק</th><th>קטגוריה</th><th>מי שילם</th><th>אמצעי</th><th>סכום</th></tr></thead><tbody>${tx.slice(0,8).map(t=>`<tr><td>${new Date(t.date).toLocaleDateString('he-IL')}</td><td><strong>${t.merchant}</strong></td><td>${state.categories.find(c=>c.id===t.categoryId)?.name||''}</td><td>${t.person}</td><td>${t.payment}</td><td><strong>${ils(t.amount)}</strong></td></tr>`).join('')}</tbody></table>`:`<div class="emptyDesk">אין עדיין עסקאות. הוסף הוצאה ראשונה כדי להתחיל.</div>`}</div></section><section class="desktopPage ${desktopTab==='budget'?'active':''}">${deskHeader('ניהול תקציב','שנה סכומים, גמישות ורזרבות ישירות מהדשבורד')}<div class="panel"><table class="budgetTable"><thead><tr><th>קבוצה</th><th>סעיף</th><th>תקציב</th><th>בפועל</th><th>גמישות</th><th>סוג</th><th></th></tr></thead><tbody>${state.categories.map(c=>`<tr><td>${c.group}</td><td><strong>${c.name}</strong></td><td>${ils(c.budget)}</td><td>${ils(actual(c.id))}</td><td>${flexLabel(c.flex)}</td><td>${c.visibility==='hidden'?'רזרבה':'גלוי'}</td><td><button class="editBtn" data-edit="${c.id}">עריכה</button></td></tr>`).join('')}</tbody></table></div></section><section class="desktopPage ${desktopTab==='transactions'?'active':''}">${deskHeader('כל ההוצאות','כל העסקאות בחודש הנוכחי')}<div class="panel">${tx.length?`<table class="budgetTable"><thead><tr><th>תאריך</th><th>עסק</th><th>קטגוריה</th><th>משלם</th><th>תשלום</th><th>סכום</th><th></th></tr></thead><tbody>${tx.map(t=>`<tr><td>${new Date(t.date).toLocaleDateString('he-IL')}</td><td>${t.merchant}</td><td>${state.categories.find(c=>c.id===t.categoryId)?.name||''}</td><td>${t.person}</td><td>${t.payment}</td><td><strong>${ils(t.amount)}</strong></td><td><button class="editBtn" data-del="${t.id}">מחיקה</button></td></tr>`).join('')}</tbody></table>`:`<div class="emptyDesk">אין עדיין הוצאות.</div>`}</div></section><section class="desktopPage ${desktopTab==='settings'?'active':''}">${deskHeader('הגדרות','מקורות הכנסה ויעדי הבית')}<div class="settingsGrid"><div class="settingCard"><h3>מקורות הכנסה</h3>${state.incomes.map(x=>`<div class="settingRow"><span>${x.name}</span><input type="number" value="${x.budget}" data-income="${x.id}"></div>`).join('')}</div><div class="settingCard"><h3>כללים</h3><div class="settingRow"><span>יעד חיסכון חודשי</span><input type="number" value="${state.household.monthlySavingsTarget}" data-setting="savings"></div><div class="settingRow"><span>חודש נוכחי</span><span>${monthLabel()}</span></div><div class="settingRow"><span>סנכרון ענן</span><span>שלב הבא: Supabase</span></div></div></div></section></main></div>`}
