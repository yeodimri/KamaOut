function alertHtml(x){const over=x.a>x.threshold;return `<div class="alert"><div><strong>${x.c.name}</strong><small>${ils(x.a)} מתוך ${ils(x.c.budget)}</small></div><span class="badge ${over?'red':'amber'}">${over?`+${ils(x.a-x.threshold)}`:`${x.p}%`}</span></div>`}function groupCard(n,g){const p=pct(g.actual,g.budget),cls=g.actual>g.budget?'over':p>=80?'warn':'';return `<div class="cat"><div class="catTop"><div><div class="catName">${n}</div><div class="catNums">${ils(g.actual)} מתוך ${ils(g.budget)}</div></div><strong>${p}%</strong></div><div class="progress ${cls}"><i style="width:${Math.min(100,p)}%"></i></div></div>`}function txHtml(t){const c=state.categories.find(c=>c.id===t.categoryId);return `<div class="transaction"><div><strong>${t.merchant}</strong><small>${c?.name||''} · ${t.person}</small></div><strong>${ils(t.amount)}</strong></div>`}
function mobileView(){const tx=txs(),al=alerts();let content='';if(mobileTab==='home')content=`<div class="top"><div><div class="brand">KamaOut</div><div class="tag">כמה יצא לנו?</div></div><div class="avatar">י</div></div><div class="monthCard"><div class="monthLabel">${monthLabel()} · הוצאות עד כה</div><div class="big">${ils(actualTotal())}</div><div class="mutedLight">מתוך תקציב חודשי ריאלי של ${ils(expenseBudget())}</div><div class="metricGrid"><div class="metric"><span>נותר בתקציב</span><strong>${ils(Math.max(expenseBudget()-actualTotal(),0))}</strong></div><div class="metric"><span>יתרה צפויה</span><strong>${ils(predictedBalance())}</strong></div></div><div class="statusPill"><i class="dot"></i>${al.some(x=>x.a>x.threshold)?'יש חריגות שדורשות תשומת לב':'התקציב כרגע מאוזן'}</div></div><div class="actions"><button class="action primary" data-action="scan">📷 צלם קבלה</button><button class="action secondary" data-action="add">＋ הוסף ידנית</button></div><div class="section"><div class="sectionHead"><h3>חריגות והתראות</h3><button class="linkBtn" data-tab="month">לכל החודש</button></div>${al.length?al.slice(0,3).map(alertHtml).join(''):`<div class="empty">אין חריגות כרגע 🎉</div>`}</div><div class="section"><div class="sectionHead"><h3>מצב לפי נושאים</h3><button class="linkBtn" data-tab="month">הכול</button></div>${Object.entries(groupData()).slice(0,5).map(([n,g])=>groupCard(n,g)).join('')}</div><div class="section"><div class="sectionHead"><h3>הוצאות אחרונות</h3><button class="linkBtn" data-tab="expenses">לכולן</button></div>${tx.length?tx.slice(0,4).map(txHtml).join(''):`<div class="empty">עדיין אין הוצאות אמיתיות.<br>הוסיפו הוצאה ראשונה כדי להתחיל.</div>`}</div>`;if(mobileTab==='month')content=`<div class="pageTitle"><h2>${monthLabel()}</h2><p>תקציב מול בפועל</p></div><div class="summaryStrip"><div class="summaryMini"><span>תקציב</span><strong>${ils(expenseBudget())}</strong></div><div class="summaryMini"><span>בפועל</span><strong>${ils(actualTotal())}</strong></div><div class="summaryMini"><span>נותר</span><strong>${ils(expenseBudget()-actualTotal())}</strong></div></div><div class="section">${state.categories.map(c=>groupCard(c.name,{budget:c.budget,actual:actual(c.id)})).join('')}</div>`;if(mobileTab==='expenses')content=`<div class="pageTitle"><h2>הוצאות</h2><p>כל העסקאות בחודש הנוכחי</p></div><div class="actions" style="margin-top:14px"><button class="action primary" data-action="scan">📷 צלם קבלה</button><button class="action secondary" data-action="add">＋ הוסף</button></div><div class="section">${tx.length?tx.map(txHtml).join(''):`<div class="empty">אין עסקאות עדיין.</div>`}</div>`;if(mobileTab==='status'){const f=flexTotals();content=`<div class="pageTitle"><h2>מצבנו</h2><p>התמונה הריאלית של משק הבית</p></div><div class="monthCard"><div class="monthLabel">יתרה חודשית צפויה</div><div class="big">${ils(predictedBalance())}</div><div class="metricGrid"><div class="metric"><span>הכנסות</span><strong>${ils(incomeBudget())}</strong></div><div class="metric"><span>הוצאות ריאליות</span><strong>${ils(expenseBudget())}</strong></div></div></div><div class="section"><div class="sectionHead"><h3>גלוי מול חבוי</h3></div>${groupCard('הוצאות גלויות',{budget:visibleBudget(),actual:0})}${groupCard('רזרבות / חבויות',{budget:hiddenBudget(),actual:0})}</div><div class="section"><div class="sectionHead"><h3>גמישות התקציב</h3></div>${groupCard('קשיח',{budget:f.fixed,actual:0})}${groupCard('בינוני',{budget:f.medium,actual:0})}${groupCard('גמיש',{budget:f.high,actual:0})}</div>`}return `<div class="mobileShell">${content}<nav class="bottomNav"><button data-tab="home" class="${mobileTab==='home'?'active':''}"><span class="navIcon">⌂</span>בית</button><button data-tab="month" class="${mobileTab==='month'?'active':''}"><span class="navIcon">▦</span>החודש</button><button data-tab="expenses" class="${mobileTab==='expenses'?'active':''}"><span class="navIcon">≡</span>הוצאות</button><button data-tab="status" class="${mobileTab==='status'?'active':''}"><span class="navIcon">◉</span>מצבנו</button></nav></div>`}
