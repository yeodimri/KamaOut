function avatarUrl(id){return window.KAMAOUT_AVATARS?.[id]||''}
function activeProfileId(){return typeof currentProfileId!=='undefined'&&currentProfileId?currentProfileId:'yehonatan'}
function activeProfileName(){return typeof profileName==='function'?profileName():activeProfileId()==='shani'?'שני':'יהונתן'}
function profileAvatar(id,cls=''){const url=avatarUrl(id);return url?`<img class="${cls}" src="${url}" alt="">`:`<span class="${cls}">${id==='shani'?'ש':'י'}</span>`}
function alertHtml(x){const over=x.a>x.threshold;return `<div class="mAlertRow"><div class="mAlertText"><strong>${x.c.name}</strong><small>${ils(x.a)} מתוך ${ils(x.c.budget)}</small></div><span class="mAlertBadge ${over?'danger':'warning'}">${over?`חריגה ${ils(x.a-x.threshold)}`:`${x.p}%`}</span></div>`}
function groupCard(n,g){const p=pct(g.actual,g.budget),cls=g.actual>g.budget?'over':p>=80?'warn':'';return `<div class="mCategoryRow"><div class="mCategoryTop"><div><strong>${n}</strong><small>${ils(g.actual)} מתוך ${ils(g.budget)}</small></div><span>${p}%</span></div><div class="mProgress ${cls}"><i style="width:${Math.min(100,p)}%"></i></div></div>`}
function fixedRow(c){return `<div class="mFixedRow"><div><strong>${c.name}</strong><small>${c.group}</small></div><strong>${ils(c.budget)}</strong></div>`}
function fixedDetails(){const items=fixedCategories();return `<div class="mFixedDetails">${items.map(fixedRow).join('')}</div>`}
function txHtml(t){const c=state.categories.find(c=>c.id===t.categoryId);return `<button class="mTxRow" data-edit-tx="${t.id}"><div class="mTxMain"><div class="mTxIcon">${(t.merchant||'ה').trim().charAt(0)}</div><div><strong>${t.merchant}</strong><small>${c?.name||''} · ${t.person||''}</small></div></div><div class="mTxRight"><strong class="mTxAmount">${ils(t.amount)}</strong><span>‹</span></div></button>`}
function mobileHome(){const tx=txs(),al=alerts(),daily=spendActual(),dailyBudget=spendBudget(),fixed=fixedBudget(),remaining=remainingSpend(),used=pct(daily,dailyBudget),groups={};spendCategories().forEach(c=>{groups[c.group]??={budget:0,actual:0};groups[c.group].budget+=Number(c.budget);groups[c.group].actual+=rawActual(c.id)});const name=activeProfileName(),pid=activeProfileId();return `<div class="mHome"><header class="mHeader"><div><div class="mWelcome">היי ${name} 👋</div><div class="mLogo">KamaOut</div><div class="mDate">${monthLabel()}</div></div><button class="mProfile" aria-label="החלפת משתמש">${profileAvatar(pid,'mProfileImg')}</button></header><section class="mHero"><div class="mHeroTop"><div><div class="mHeroEyebrow">הוצאות שוטפות החודש</div><div class="mHeroAmount">${ils(daily)}</div></div><span class="mStatusPill ${daily>dailyBudget?'bad':used>=80?'warn':'good'}">${daily>dailyBudget?'בחריגה':used>=80?'מתקרבים':'בשליטה'}</span></div><div class="mHeroTrack"><i style="width:${Math.min(100,used)}%"></i></div><div class="mHeroBudget"><span>${used}% מהתקציב השוטף</span><span>נשאר ${ils(remaining)}</span></div><div class="mHeroDivider"></div><div class="mHeroStats"><div><span>קבועות</span><strong>${ils(fixed)}</strong></div><div><span>שוטף מתוכנן</span><strong>${ils(dailyBudget)}</strong></div><div><span>סה״כ כרגע</span><strong>${ils(actualTotal())}</strong></div></div></section><div class="mQuickActions"><button class="mQuick primary" data-action="scan"><span class="mQuickIcon">▣</span><span><strong>צלם קבלה</strong><small>סופר, דלק, קניות ועוד</small></span></button><button class="mQuick secondary" data-action="add"><span class="mQuickIcon">＋</span><span><strong>הוסף ידנית</strong><small>הוצאה שוטפת בכמה שניות</small></span></button></div><section class="mBlock"><div class="mBlockHead"><div><span class="mKicker">מצב השוטף</span><h3>חריגות והתראות</h3></div><button data-tab="month">לכל התקציב</button></div>${al.length?al.slice(0,3).map(alertHtml).join(''):`<div class="mGoodState"><span class="mGoodIcon">✓</span><div><strong>הכול נראה טוב</strong><small>אין כרגע חריגות בהוצאות השוטפות</small></div></div>`}</section><section class="mBlock"><div class="mBlockHead"><div><span class="mKicker">איפה מוציאים בפועל?</span><h3>הוצאות שוטפות</h3></div><button data-tab="month">הכול</button></div>${Object.entries(groups).sort((a,b)=>b[1].budget-a[1].budget).slice(0,5).map(([n,g])=>groupCard(n,g)).join('')}</section><section class="mBlock"><div class="mBlockHead"><div><span class="mKicker">יורדות אוטומטית מהחודש</span><h3>הוצאות קבועות</h3></div><button data-fixed-toggle>${mobileFixedOpen?'סגור':'פירוט'}</button></div><div class="mFixedSummary"><span>לא דורש מעקב או קבלות</span><strong>${ils(fixed)}</strong></div>${mobileFixedOpen?fixedDetails():''}<small class="mBlockNote">חינוך, משכנתא, חשמל, ביטוחים, נסיעות קבועות, בריאות ועוד.</small></section><section class="mBlock mLastBlock"><div class="mBlockHead"><div><span class="mKicker">פעילות</span><h3>הוצאות אחרונות</h3></div><button data-tab="expenses">לכולן</button></div>${tx.length?tx.slice(0,5).map(txHtml).join(''):`<div class="mEmptyState"><div class="mEmptyIcon">₪</div><strong>עוד אין הוצאות שוטפות</strong><small>קנייה, דלק או סופר שתוסיפו יופיעו כאן מיד</small><button data-action="add">הוסף הוצאה</button></div>`}</section></div>`}
function mobileMonth(){const fixed=fixedCategories(),spend=spendCategories();return `<div class="mSubPage"><div class="mSubHeader"><span>${monthLabel()}</span><h2>החודש</h2><p>קבועות מול הוצאות שוטפות</p></div><div class="mMiniCards"><div><span>קבועות</span><strong>${ils(fixedBudget())}</strong></div><div><span>שוטף בפועל</span><strong>${ils(spendActual())}</strong></div><div><span>נשאר לשוטף</span><strong>${ils(remainingSpend())}</strong></div></div><section class="mBlock mStandalone"><div class="mBlockHead"><div><span class="mKicker">מה שאנחנו מזינים</span><h3>שוטפות</h3></div></div>${spend.map(c=>groupCard(c.name,{budget:c.budget,actual:rawActual(c.id)})).join('')}</section><section class="mBlock"><div class="mBlockHead"><div><span class="mKicker">ללא צורך בהזנה</span><h3>קבועות</h3></div></div>${fixed.map(fixedRow).join('')}</section></div>`}
function mobileExpenses(){const tx=txs();return `<div class="mSubPage"><div class="mSubHeader"><span>${monthLabel()}</span><h2>הוצאות שוטפות</h2><p>קניות, סופר, דלק, בגדים, בילויים ועוד</p></div><div class="mQuickActions mSubActions"><button class="mQuick primary" data-action="scan"><span class="mQuickIcon">▣</span><span><strong>צלם קבלה</strong></span></button><button class="mQuick secondary" data-action="add"><span class="mQuickIcon">＋</span><span><strong>הוסף ידנית</strong></span></button></div><section class="mBlock mStandalone">${tx.length?tx.map(txHtml).join(''):`<div class="mEmptyState"><div class="mEmptyIcon">₪</div><strong>אין עסקאות שוטפות עדיין</strong><small>הוסיפו קנייה והיא תופיע כאן.</small></div>`}</section></div>`}
function mobileStatus(){return `<div class="mSubPage"><div class="mSubHeader"><span>${monthLabel()}</span><h2>מצבנו</h2><p>התמונה הריאלית של משק הבית</p></div><section class="mBalanceCard"><span>יתרה חודשית צפויה</span><strong>${ils(predictedBalance())}</strong><small>${ils(incomeBudget())} הכנסות פחות ${ils(expenseBudget())} תקציב מלא</small></section><section class="mBlock mStandalone"><div class="mBlockHead"><div><span class="mKicker">מבנה התקציב</span><h3>קבוע מול שוטף</h3></div></div>${groupCard('הוצאות קבועות',{budget:expenseBudget(),actual:fixedBudget()})}${groupCard('תקציב שוטף',{budget:expenseBudget(),actual:spendBudget()})}</section><section class="mBlock"><div class="mBlockHead"><div><span class="mKicker">החודש בפועל</span><h3>מה כבר יצא</h3></div></div><div class="mFixedSummary"><span>קבועות + שוטפות שנרשמו</span><strong>${ils(actualTotal())}</strong></div></section></div>`}
function mobileView(){const content=mobileTab==='home'?mobileHome():mobileTab==='month'?mobileMonth():mobileTab==='expenses'?mobileExpenses():mobileStatus();return `<div class="mobileShell">${content}<nav class="bottomNav"><button data-tab="home" class="${mobileTab==='home'?'active':''}"><span class="navIcon">⌂</span><span>בית</span></button><button data-tab="month" class="${mobileTab==='month'?'active':''}"><span class="navIcon">▦</span><span>החודש</span></button><button data-tab="expenses" class="${mobileTab==='expenses'?'active':''}"><span class="navIcon">≡</span><span>הוצאות</span></button><button data-tab="status" class="${mobileTab==='status'?'active':''}"><span class="navIcon">◉</span><span>מצבנו</span></button></nav></div>`}
